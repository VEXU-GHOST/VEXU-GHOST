#include "yudhish_onboarding/calculator.hpp"

namespace yudhish_onboarding {
    int Calculator::add(int a, int b) const { 
        return a + b;
    }
    int Calculator::square(int x) const {
         return x * x;
    }
}